# AUTO-GENERATED by scripts/generate-compose.ts — do not edit by hand.
# Regenerate: bun run generate-compose

name: chrisvouga-dev

networks:
  web:
    name: chrisvouga-dev-web

volumes:
  netdatacache:
  netdataconfig:
  netdatalib:

services:
  traefik:
    image: traefik:v3.6.6
    restart: unless-stopped
    command:
      - --configFile=/etc/traefik/traefik.yml
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./traefik/dynamic:/etc/traefik/dynamic:ro
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.middlewares.cfproto.headers.customrequestheaders.X-Forwarded-Proto=https

  portfolio:
    image: ghcr.io/crvouga/chrisvouga-dev-portfolio:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.portfolio.rule=Host(`www.chrisvouga.dev`)
      - traefik.http.routers.portfolio.entrypoints=web
      - traefik.http.routers.portfolio.middlewares=cfproto@docker
      - traefik.http.services.portfolio.loadbalancer.server.port=80
  pickflix:
    image: ghcr.io/crvouga/chrisvouga-dev-pickflix:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/pickflix.env
    environment:
      PORT: "9000"
    labels:
      - traefik.enable=true
      - traefik.http.routers.pickflix.rule=Host(`pickflix.chrisvouga.dev`)
      - traefik.http.routers.pickflix.entrypoints=web
      - traefik.http.routers.pickflix.middlewares=cfproto@docker
      - traefik.http.services.pickflix.loadbalancer.server.port=9000
  moviefinder-app-rust:
    image: ghcr.io/crvouga/chrisvouga-dev-moviefinder-app-rust:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/moviefinder-app-rust.env
    environment:
      PORT: "3000"
    labels:
      - traefik.enable=true
      - traefik.http.routers.moviefinderapprust.rule=Host(`moviefinder-app-rust.chrisvouga.dev`)
      - traefik.http.routers.moviefinderapprust.entrypoints=web
      - traefik.http.routers.moviefinderapprust.middlewares=cfproto@docker
      - traefik.http.services.moviefinderapprust.loadbalancer.server.port=3000
  moviefinder-app-go:
    image: ghcr.io/crvouga/chrisvouga-dev-moviefinder-app-go:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/moviefinder-app-go.env
    environment:
      PORT: "8080"
    labels:
      - traefik.enable=true
      - traefik.http.routers.moviefinderappgo.rule=Host(`moviefinder-app-go.chrisvouga.dev`)
      - traefik.http.routers.moviefinderappgo.entrypoints=web
      - traefik.http.routers.moviefinderappgo.middlewares=cfproto@docker
      - traefik.http.services.moviefinderappgo.loadbalancer.server.port=8080
  moviefinder-app-react:
    image: ghcr.io/crvouga/chrisvouga-dev-moviefinder-app-react:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/moviefinder-app-react.env
    environment:
      PORT: "3000"
    labels:
      - traefik.enable=true
      - traefik.http.routers.moviefinderappreact.rule=Host(`moviefinder-app-react.chrisvouga.dev`)
      - traefik.http.routers.moviefinderappreact.entrypoints=web
      - traefik.http.routers.moviefinderappreact.middlewares=cfproto@docker
      - traefik.http.services.moviefinderappreact.loadbalancer.server.port=3000
  moviefinder-app-clojurescript:
    image: ghcr.io/crvouga/chrisvouga-dev-moviefinder-app-clojurescript:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/moviefinder-app-clojurescript.env
    environment:
      PORT: "9630"
    labels:
      - traefik.enable=true
      - traefik.http.routers.moviefinderappclojurescript.rule=Host(`moviefinder-app-clojurescript.chrisvouga.dev`)
      - traefik.http.routers.moviefinderappclojurescript.entrypoints=web
      - traefik.http.routers.moviefinderappclojurescript.middlewares=cfproto@docker
      - traefik.http.services.moviefinderappclojurescript.loadbalancer.server.port=9630
  headless-combobox-svelte-example:
    image: ghcr.io/crvouga/chrisvouga-dev-headless-combobox-svelte-example:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.headlesscomboboxsvelteexample.rule=Host(`svelte-headlesscombobox.chrisvouga.dev`)
      - traefik.http.routers.headlesscomboboxsvelteexample.entrypoints=web
      - traefik.http.routers.headlesscomboboxsvelteexample.middlewares=cfproto@docker
      - traefik.http.services.headlesscomboboxsvelteexample.loadbalancer.server.port=80
  headless-combobox-docs:
    image: ghcr.io/crvouga/chrisvouga-dev-headless-combobox-docs:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.headlesscomboboxdocs.rule=Host(`headlesscombobox.chrisvouga.dev`)
      - traefik.http.routers.headlesscomboboxdocs.entrypoints=web
      - traefik.http.routers.headlesscomboboxdocs.middlewares=cfproto@docker
      - traefik.http.services.headlesscomboboxdocs.loadbalancer.server.port=80
  todo-app:
    image: ghcr.io/crvouga/chrisvouga-dev-todo-app:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.todoapp.rule=Host(`todo.chrisvouga.dev`)
      - traefik.http.routers.todoapp.entrypoints=web
      - traefik.http.routers.todoapp.middlewares=cfproto@docker
      - traefik.http.services.todoapp.loadbalancer.server.port=80
  image-service:
    image: ghcr.io/crvouga/chrisvouga-dev-image-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.imageservice.rule=Host(`imageservice.chrisvouga.dev`)
      - traefik.http.routers.imageservice.entrypoints=web
      - traefik.http.routers.imageservice.middlewares=cfproto@docker
      - traefik.http.services.imageservice.loadbalancer.server.port=80
  connect-four:
    image: ghcr.io/crvouga/chrisvouga-dev-connect-four:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.connectfour.rule=Host(`connectfour.chrisvouga.dev`)
      - traefik.http.routers.connectfour.entrypoints=web
      - traefik.http.routers.connectfour.middlewares=cfproto@docker
      - traefik.http.services.connectfour.loadbalancer.server.port=80
  anime-blog:
    image: ghcr.io/crvouga/chrisvouga-dev-anime-blog:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.animeblog.rule=Host(`anime.chrisvouga.dev`)
      - traefik.http.routers.animeblog.entrypoints=web
      - traefik.http.routers.animeblog.middlewares=cfproto@docker
      - traefik.http.services.animeblog.loadbalancer.server.port=80
  snake-game:
    image: ghcr.io/crvouga/chrisvouga-dev-snake-game:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.snakegame.rule=Host(`snake.chrisvouga.dev`)
      - traefik.http.routers.snakegame.entrypoints=web
      - traefik.http.routers.snakegame.middlewares=cfproto@docker
      - traefik.http.services.snakegame.loadbalancer.server.port=80
  match-three:
    image: ghcr.io/crvouga/chrisvouga-dev-match-three:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.matchthree.rule=Host(`matchthree.chrisvouga.dev`)
      - traefik.http.routers.matchthree.entrypoints=web
      - traefik.http.routers.matchthree.middlewares=cfproto@docker
      - traefik.http.services.matchthree.loadbalancer.server.port=80
  simon-says:
    image: ghcr.io/crvouga/chrisvouga-dev-simon-says:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.simonsays.rule=Host(`simonsays.chrisvouga.dev`)
      - traefik.http.routers.simonsays.entrypoints=web
      - traefik.http.routers.simonsays.middlewares=cfproto@docker
      - traefik.http.services.simonsays.loadbalancer.server.port=80
  vault:
    image: ghcr.io/crvouga/chrisvouga-dev-vault:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/vault.env
    environment:
      BAO_API_ADDR: "https://vault.chrisvouga.dev"
    labels:
      - traefik.enable=true
      - traefik.http.routers.vault.rule=Host(`vault.chrisvouga.dev`)
      - traefik.http.routers.vault.entrypoints=web
      - traefik.http.routers.vault.middlewares=cfproto@docker
      - traefik.http.services.vault.loadbalancer.server.port=8200
  turborepo:
    image: ghcr.io/crvouga/chrisvouga-dev-turborepo:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/turborepo.env
    environment:
      PORT: "8080"
      VAULT_ADDR: "http://vault:8200"
      VAULT_PROJECT: "personal"
      VAULT_CONFIG: "prd"
    depends_on:
      - vault
    labels:
      - traefik.enable=true
      - traefik.http.routers.turborepo.rule=Host(`turborepo.chrisvouga.dev`)
      - traefik.http.routers.turborepo.entrypoints=web
      - traefik.http.routers.turborepo.middlewares=cfproto@docker
      - traefik.http.services.turborepo.loadbalancer.server.port=8080
  normalizer:
    image: ghcr.io/crvouga/chrisvouga-dev-normalizer:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/normalizer.env
    environment:
      PORT: "8080"
      NODE_ENV: "production"
      SERVER_BASE_URL: "https://normalizer.chrisvouga.dev"
    labels:
      - traefik.enable=true
      - traefik.http.routers.normalizer.rule=Host(`normalizer.chrisvouga.dev`)
      - traefik.http.routers.normalizer.entrypoints=web
      - traefik.http.routers.normalizer.middlewares=cfproto@docker
      - traefik.http.services.normalizer.loadbalancer.server.port=8080
  llm-proxy:
    image: ghcr.io/crvouga/chrisvouga-dev-llm-proxy:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/llm-proxy.env
    environment:
      PORT: "8080"
    labels:
      - traefik.enable=true
      - traefik.http.routers.llmproxy.rule=Host(`llm-proxy.chrisvouga.dev`)
      - traefik.http.routers.llmproxy.entrypoints=web
      - traefik.http.routers.llmproxy.middlewares=cfproto@docker
      - traefik.http.services.llmproxy.loadbalancer.server.port=8080
  normalizer-worker:
    image: ghcr.io/crvouga/chrisvouga-dev-normalizer-worker:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - web
    env_file:
      - ./env/normalizer-worker.env
    environment:
      NODE_ENV: "production"
    depends_on:
      - normalizer

  netdata:
    image: netdata/netdata:stable
    restart: unless-stopped
    networks:
      - web
    pid: host
    cap_add:
      - SYS_PTRACE
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    volumes:
      - netdataconfig:/etc/netdata
      - netdatalib:/var/lib/netdata
      - netdatacache:/var/cache/netdata
      - /etc/passwd:/host/etc/passwd:ro
      - /etc/group:/host/etc/group:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /etc/os-release:/host/etc/os-release:ro
      - /var/log:/host/var/log:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.netdata.rule=Host(`netdata.chrisvouga.dev`)
      - traefik.http.routers.netdata.entrypoints=web
      - traefik.http.routers.netdata.middlewares=cfproto@docker,netdata-auth@file
      - traefik.http.services.netdata.loadbalancer.server.port=19999
  dozzle:
    image: amir20/dozzle:v8
    restart: unless-stopped
    networks:
      - web
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./dozzle/users.yml:/data/users.yml:ro
    environment:
      DOZZLE_HOSTNAME: "dozzle.chrisvouga.dev"
      DOZZLE_AUTH_PROVIDER: "simple"
      DOZZLE_NO_ANALYTICS: "true"
    labels:
      - traefik.enable=true
      - traefik.http.routers.dozzle.rule=Host(`dozzle.chrisvouga.dev`)
      - traefik.http.routers.dozzle.entrypoints=web
      - traefik.http.routers.dozzle.middlewares=cfproto@docker
      - traefik.http.services.dozzle.loadbalancer.server.port=8080