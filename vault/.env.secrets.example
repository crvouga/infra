# Optional overrides — leave blank to auto-fetch from CLI logins where supported
# Copy to .env.secrets only when needed

# Required: Cloudflare Zone:DNS:Edit API token (Wrangler OAuth cannot be used)
CLOUDFLARE_API_TOKEN=

NEON_PROJECT_ID=
CF_API_TOKEN=
DB_CONNECTION_URI=
VAULT_TOKEN=
