# Runtime on the node (written by CI — never commit real values).
IMAGE_TAG=latest

# Per-service secrets live in env/<service-id>.env (CI-managed).

# Local scripts only (stored in Vault for CI):
# DIGITALOCEAN_TOKEN=
# GITHUB_TOKEN_SUPER=
# NODE_SSH_HOST=
# NODE_SSH_USER=root
# NODE_SSH_KEY=
